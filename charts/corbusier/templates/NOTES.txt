Corbusier has been deployed!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
{{- $serviceName := include "corbusier.fullname" . -}}
{{- $servicePort := .Values.service.port -}}
{{- $selector := printf "app.kubernetes.io/name=%s,app.kubernetes.io/instance=%s" (include "corbusier.name" .) .Release.Name -}}
{{- $hosts := .Values.ingress.hosts | default list -}}
{{- $firstHost := "" -}}
{{- if gt (len $hosts) 0 -}}
{{- $firstHost = (index $hosts 0).host | default "" -}}
{{- end }}

{{- if and .Values.ingress.enabled $firstHost }}

Access the application:
  http://{{ $firstHost }}/
{{- else }}

Access the application with port-forwarding:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ $serviceName }} {{ $servicePort }}:{{ $servicePort }}

Then inspect workload state:

  kubectl get pods -n {{ .Release.Namespace }} -l "{{ $selector }}"

Then stream logs:

  kubectl logs -n {{ .Release.Namespace }} -l "{{ $selector }}" -f
{{- end }}

To access via port-forward:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ $serviceName }} {{ $servicePort }}:{{ $servicePort }}

Then open: http://localhost:{{ $servicePort }}

Then verify the probes:

  curl -f http://localhost:{{ $servicePort }}/health/live
  curl -f http://localhost:{{ $servicePort }}/health/ready

To check pod status:

  kubectl get pods -n {{ .Release.Namespace }} -l "{{ $selector }}"

To view logs:

  kubectl logs -n {{ .Release.Namespace }} -l "{{ $selector }}" -f
