Corbusier has been deployed!

Release: {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}

Access the application:
{{- with .Values.ingress.hostname }}
  http://{{ . }}/
{{- else }}
  http://<cluster-ip>/
  (Using hostless ingress - access via cluster load balancer)
{{- end }}
{{- else }}

The application is not exposed via Ingress.
{{- end }}

To access via port-forward:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "corbusier.fullname" . }} 8080:{{ .Values.service.port }}

Then open: http://localhost:8080

To check pod status:

  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "corbusier.name" . }},app.kubernetes.io/instance={{ .Release.Name }}"

To view logs:

  kubectl logs -n {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "corbusier.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -f
